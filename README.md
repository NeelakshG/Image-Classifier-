# Image-Classifier-
Image classification system that explicitly models in-domain vs. out-of-domain data using a custom CNN, hybrid training loss, and post-training uncertainty analysis to reduce overconfidence under distribution shift.
